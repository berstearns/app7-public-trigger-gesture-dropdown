# PLACEHOLDER — real values come from env vars (see .env.template at repo root).
# androidApp/build.gradle.kts reads env vars first; this file is the fallback.
backend_url: "http://PLACEHOLDER_BACKEND_HOST:8080"
queue_host: "PLACEHOLDER_QUEUE_HOST"
queue_port: 0
